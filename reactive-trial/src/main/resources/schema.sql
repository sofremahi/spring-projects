CREATE TABLE IF NOT EXISTS product (
                                       id VARCHAR(255) PRIMARY KEY,
                                       name VARCHAR(255) NOT NULL,
                                       price INT NOT NULL
);